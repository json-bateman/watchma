meta {
  name: AllMovies
  type: http
  seq: 2
}

get {
  url: {{JELLYFIN_BASE_URL}}/Items?IncludeItemTypes=Movie&Recursive=true&fields=Genres
  body: none
  auth: inherit
}

params:query {
  IncludeItemTypes: Movie
  Recursive: true
  fields: Genres
}

headers {
  X-Emby-Token: {{JELLYFIN_API_KEY}}
}
